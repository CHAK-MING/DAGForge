# Optional. If omitted, frontend uses relative /api (recommended with Vite proxy).
# VITE_API_BASE=http://127.0.0.1:8888/api
# Optional. Set false to disable websocket completely.
# VITE_WS_ENABLED=true
# Optional: override WebSocket endpoint directly.
# VITE_WS_URL=ws://127.0.0.1:8888/ws
